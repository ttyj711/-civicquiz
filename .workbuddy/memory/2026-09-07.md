# 2026-09-07 工作日志

## 安装 CloudBase Skills (civicquiz 项目)
- 目标：`npx skills add tencentcloudbase/cloudbase-skills -y`
- 根因坑：本地 git 全局代理 `127.0.0.1:7897` 已失效（对全站返回 502 / GitHub HTTPS TLS 握手失败）。实测 GitHub 当前可直连，绕过代理即可。
- 解法：`GIT_CONFIG_GLOBAL=/dev/null` 让 npx 内部 git 直连克隆。
- 并发冲突：之前被用户中断的运行残留僵尸进程 + 后台自动运行的实例，两个 `skills add` 同时写 `.agents/skills/cloudbase` 互相清空（SKILL.md 变 0 字节）。
- 最终落地：手动克隆仓库（项目内 `_cbsk_clone`）→ 复制到 `.agents/skills/cloudbase` → 清理临时目录。
- 结果：`G:\git_code\civicquiz\.agents\skills\cloudbase\` 共 137 文件、SKILL.md 16495 字节、v2.33.0、28 个子技能 reference。
- 清理：删除 Windows Temp 下 `skills-LQuCRE`/`skills-UYkTdd`/`skills-zWYJb8` 三个残留临时克隆目录。项目与 Temp 均干净。
- 可复用提示：该 git 代理失效 → 直连绕过 已写入跨项目 `~/.workbuddy/MEMORY.md`。

## M0 基础设施（刷题小程序开发计划获批后执行）
- 开发计划定稿于 `C:\Users\z\.workbuddy\plans\radiant-vortex-darwin-clWQFSkz.md`（M0-M6 里程碑+任务清单）。
- **后端技术栈三次变更最终定案：Nuxt4 → NestJS → Nitro（官方 starter）**。计划文档已同步。
- pnpm 9.15.9（经 managed node 全局安装，装在 F:\Nodejs\node_cache）。registry=npmmirror 可用。
- **环境坑：WorkBuddy 注入的 NODE_OPTIONS safe-delete shim 会拦截 pnpm 清理临时目录**（SAFE_DELETE_BULK_CONFIRM_REQUIRED）→ 跑 pnpm 前 `unset NODE_OPTIONS`。
- Monorepo 落位：pnpm workspace（apps/* + packages/*）。
  - apps/server：Nitro 官方 starter（nitrojs/starter `cli` 分支，v3：`nitro` 包 + defineHandler + serverDir=./server）。`create-nitro-app` 内置 registry 404 过期，直连克隆分支取模板。`GET /api → {"message":"Hello from API!"}` 验证通过。
  - apps/admin：Vite react-ts 官方模板 + antd6/@tanstack/react-query5/react-router-dom7。
  - apps/miniapp：Taro 4.2.1 官方 CLI（React+TS+Sass+Webpack5）。
  - packages/shared：types.ts(题型/模式/DTO)、schemas.ts(zod)、index.ts。
- **Taro CLI headless bug**：`--autoInstall=false` 字符串→napi bool 转换崩溃（`--npm`/`--css` 枚举也只认首字母大写 Npm/Sass）；winpty 管道下 ES5 confirm 之外的匿名 bool 崩溃同源。解法：去掉 autoInstall 参数 + `scripts/taro-init-driver.cjs`（node-pty 驱动，按提示词应答，y/N 一律 n）。node-pty@1.1.0 prebuilt 可用。
- GitHub 直连间歇性抖动：clone 失败时重试即可（带 GIT_CONFIG_GLOBAL=/dev/null）。
- **CloudBase 决策 A 验证门结果**：现有环境 `cloudbase-d5gixk4cj16730bf9`（个人版，NoSQL 后端）无 PostgreSQL 且不可原地升级；用户拍板「新建 PG 模式环境」→ 创建 `civicquiz` 环境（baas_personal ¥39.9/月, resources 含 postgresql）**因「账户余额不足」下单失败**，待用户充值后重试（MCP manageEnv create 已就绪）。
- M0 状态：三端脚手架 + workspace + shared 包 ✅；Nitro `GET /api` 验证 ✅；miniapp 依赖安装 ✅（webpack5-runner 有无害的 less peer 警告）；CloudBase PG 环境 ⏳ 等充值。

## 用户拍板「先本地开发」→ M0/M1 当日完成
- 本地数据库：`embedded-postgres@18.1.0-beta.15` + `@embedded-postgres/windows-x64@18.1.0-beta.15`（注意：核心包 18.4.0-beta.17 与 windows 平台包版本错配导致 optional 被跳过，必须配对 18.1.0-beta.15）。数据目录 `.pgdata/`（已 gitignore），端口 55432，库 civicquiz/civicquiz。
- `scripts/dev-db.cjs start|stop|status` 启停；PostgreSQL 18.1 已常驻后台。
- **M1 数据层完成**：`cloudbase/migrations/20260907171000_init_core_tables.sql`（14 核心表 + sys_admin + updated_at 触发器，CloudBase 迁移命名兼容）；`scripts/migrate.cjs` 执行器（schema_migrations 记录版本、事务执行、单次应用）——一次通过，16 表落库。
- `scripts/seed.cjs` 幂等 seed：admin/admin123 + 「时政热点题库」+ 3 分类 + 20 道时政单选题（80 选项/20 答案），复跑 inserted=0。
- server 加 `server/utils/db.ts`（pg Pool + tx 事务封装）与 `/api/health`（SELECT 1）→ `{"ok":true,"db":true}` 全链路验证通过。
- 任务状态：M0 ✅、M1 ✅；下一步 M2 后端 API。



